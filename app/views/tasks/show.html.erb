<h1><%= @task.title %></h1>
  <p><%= @task.details %></p>
<p><%= @task.completed == true ? "[X] This task is completed" : "[ ] This task is not completed yet" %></p>
<%= link_to 'Back to list', tasks_path %>
<%= link_to 'Edit task', edit_task_path(@task) %>
